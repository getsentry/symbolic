[package]
name = "symbolic-debuginfo"
version = "5.8.0"
license = "MIT"
authors = [
    "Armin Ronacher <armin.ronacher@active-4.com>",
    "Jan Michael Auer <mail@jauer.org>",
]
documentation = "https://docs.rs/symbolic-debuginfo"
homepage = "https://github.com/getsentry/symbolic"
repository = "https://github.com/getsentry/symbolic"
description = """
A library to inspect and load DWARF debugging information from binaries, such
as Mach-O or ELF.
"""
edition = "2018"

[dependencies]
dmsort = "1.0.0"
failure = "0.1.5"
fallible-iterator = "0.1.6"
flate2 = { version = "1.0.6", features = ["rust_backend"], default-features = false }
fnv = "1.0.6"
gimli = "0.16.1"
goblin = "0.0.20"
# lazy_static = "1.2.0"
lru-cache = "0.1.1"
# owning_ref = "0.4.0"
# TODO(ja): Use proper dependency
pdb = { git = "https://github.com/willglynn/pdb" }
pest = "2.1.0"
pest_derive = "2.1.0"
# regex = "1.1.0"
# serde = { version = "1.0.80", optional = true }
# serde_plain = { version = "0.3.0", optional = true }
smallvec = "0.6.8"
symbolic-common = { path = "../common", version = "5.8.0" }

[dev-dependencies]
symbolic-testutils = { version = "5.8.0", path = "../testutils" }

[features]
default = []
# w_serde = ["serde", "serde_plain"]
