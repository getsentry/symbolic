language: rust
dist: trusty
sudo: required

services:
  - docker

rust:
  - stable
  - beta
  - nightly

matrix:
  allow_failures:
    - rust: nightly

before_script:
  - docker pull multiarch/crossbuild:latest

script: make
